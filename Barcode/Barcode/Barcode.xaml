<Window x:Class="BarCodeExample.Barcode"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStartupLocation="CenterScreen"
        Title="Barcode Example" Height="359.036" Width="782.877" Background="{x:Null}">
    <Grid Grid.Row="1">
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <DockPanel Grid.ColumnSpan="1">
            <Grid DockPanel.Dock="Bottom">
                <Grid.Background>
                    <ImageBrush ImageSource="Images/formbackground.jpeg"/>
                </Grid.Background>
                <Label x:Name="lblBarcode"  Content="Barcode with C#" Width="211" Margin="282,5,282,217" FontSize="24"/>
                <Canvas x:Name="mainCanvas" HorizontalAlignment="Left" Height="156" Margin="71,65,0,0" VerticalAlignment="Top" Width="650" Background="{x:Null}"></Canvas>
            </Grid>
        </DockPanel>
    </Grid>
</Window>